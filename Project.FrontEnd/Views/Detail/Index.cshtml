@using Microsoft.Extensions.Configuration
@using Project.Proxy.ViewModels
@using Microsoft.Extensions.Configuration
@inject IConfiguration Configuration
@model ProductViewModel;
@{
    ViewData["Title"] = @Model.Title;
    ViewData["Description"] = @Model.Description;
    ViewData["Imags"] = @Configuration.GetSection("Domain")["FrontEnd"] + "/imgs/default.jpg";
}
<section class="blog-hero spad">
    <div class="container">
        <div class="row d-flex justify-content-center">
            <div class="col-lg-9 text-center">
                <div class="blog__hero__text">
                    <h2>@Model.Title</h2>
                    <ul>
                        <li>By Admin</li>
                        <li>@Model.DateCreated.ToString("dd-MM-yyyy")</li>
                        <li>8 bình luận</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>
<section class="blog-details spad">
    <div class="container">
        <div class="row d-flex justify-content-center">
            <div class="col-lg-12">
                <div class="blog__details__pic">
                    <img src="~/imgs/trung-tam-thi-blx-a1-a2.jpg" alt="trung tâm"/>
                </div>
            </div>
            <div class="col-lg-8">
                <div class="blog__details__content">
                    @if (!string.IsNullOrEmpty(Model.Body)){
                    @Html.Raw(Model.Body)
                    }

                </div>
            </div>
        </div>
    </div>
</section>
@* <div class="container mt-5">
    <main role="main" class="pb-3">
        <div class="row">
            <div class="col-md-9">

                <nav class="category-detail">
                    <h3></h3>
                    <div class="category-description">@Model.Description</div>
                    <div>
                    </div>
                </nav>
             



            </div>
            <div class="col-md-3 ">
                @await Component.InvokeAsync("CategoryHot")
            </div>
        </div>
    </main>
</div> *@